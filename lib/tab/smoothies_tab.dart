import 'package:flutter/material.dart';

class SmoothiesTab extends StatelessWidget {
  const SmoothiesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Smoothies Tab Content'));
  }
}
